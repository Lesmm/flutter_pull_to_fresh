import 'package:flutter/material.dart';

class PageOfDiscovery extends StatelessWidget {
  const PageOfDiscovery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Discovery'),
    );
  }
}
