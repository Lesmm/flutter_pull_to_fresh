import 'package:flutter/material.dart';

class PageOfMe extends StatelessWidget {
  const PageOfMe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Me'),
    );
  }
}
